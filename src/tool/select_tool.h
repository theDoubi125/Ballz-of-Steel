#ifndef SELECT_TOOL_H_INCLUDED
#define SELECT_TOOL_H_INCLUDED

#include "tool.h"

/*class SelectTool : public Tool
{
public:
    SelectTool(World &world, std::vector<Player*> Players, Vec pos, Vec dim, std::vector<Tool*> *others);
    virtual ~SelectTool();

    virtual void updateTool(const PlayerController &event);
    virtual void useTool(World &world, const PlayerController &event);
    virtual void draw(sf::RenderTarget &window, const PlayerController &event) const;

private:
    Vec m_A, m_B;
    bool m_select;
};*/

#endif // SELECT_TOOL_H_INCLUDED
